// LOG N RUNNING TIME

bool checkDuplicates(vector<int>arr)
  {
    for(int i=0;i<arr.size();i++)
      {
        if(abs(arr[i])<0)
        else
        {
          arr[arr[i]]=-arr[arr[i]];
        } 


      }

  }
